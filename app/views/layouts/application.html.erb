<!DOCTYPE html>
<html>
  <head>
    <title>UrlShortener</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <!-- Production -->
    <script src="https://unpkg.com/@popperjs/core@2"></script>
    <script src="https://unpkg.com/tippy.js@6"></script>
  </head>

  <body>
    <main class="prose mx-auto mt-28 px-5">
      <% if alert %>
        <p class="bg-yellow-100 text-yellow-900 text-sm font-medium px-4 py-2 rounded"><%= alert %></p>
      <% end %> <!--      the 1st way to do, straight-->
      <%= tag.p notice, class: "bg-blue-100 text-blue-900 text-sm font-medium px-4 py-2 rounded" if notice%>
      <%= yield %>
    </main>
  </body>
</html>
