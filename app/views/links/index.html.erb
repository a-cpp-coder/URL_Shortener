<h1>URL Shortener</h1>

<%= render partial: 'form', locals: {link: @link} %>

<div class="mt-8 flex flex-col gap-4 divide-y divide-gray-100">
  <%= render @links %>  <!-- render the instance variable @links in links_controller.rb -->
</div>
